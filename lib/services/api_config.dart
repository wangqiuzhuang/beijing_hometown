/// DeepSeek API 配置
///
/// 🔑 获取你的 API Key：https://platform.deepseek.com/api_keys
///    （新用户注册即送免费额度）
///
/// ⚠️ 安全提示：
///    设置真实 Key 后，运行以下命令防止 Key 被提交：
///    git update-index --skip-worktree lib/services/api_config.dart
///
class ApiConfig {
  /// 将此处的占位符替换为你的 DeepSeek API Key
  /// 如果保持占位符不变，AI 功能会提示需要配置 Key，不影响其他功能正常使用
  static const String deepseekKey = 'YOUR_DEEPSEEK_API_KEY_HERE';
}
